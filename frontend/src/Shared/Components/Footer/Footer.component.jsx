import React from "react";
import { Layout } from "antd";
const { Footer } = Layout;

function FooterComponent() {
  return (
    <Footer style={{ textAlign: "center" }}>
      <strong>Julian Valencia</strong>
    </Footer>
  );
}

export default FooterComponent;
